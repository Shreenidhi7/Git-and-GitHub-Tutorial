
Continuation from previous section, Ideally when we already have a repo it GitHub/initialized the repo in GitHub and then we want to start our project after creating the remote repo.
What is the scenario for that?
- Lets discuss about that scenario now.
- That is equally important
- In most of the cases, we first create a remote repo and then we clone it and then we just do our development and then push the changes

1. We shall create a new folder basically from scratch.
2. We shall clone the repository and then we shall do some development

We shall start a new project with the repo which is existing in GITHUB
File Explorer -> Create a new folder -> Shrn Remote Clone